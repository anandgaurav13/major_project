# Copy this file to ".env.local" in the SAME folder as package.json, then restart "npm run dev".
# Next.js does NOT load .env.example automatically.
#
# Google Cloud: enable "Maps JavaScript API" and "Directions API" for this key.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
